# Repo-local APEX context (ADR-0012): makes APEX service-aware in this repo.
service: {{service_name}}
team: {{team}}
template: web-api
template_version: 0.1.0
gitops_path: teams/{{team}}/{{service_name}}
